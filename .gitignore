# Legacy dense model (the app now builds a compact sparse index at runtime)
similarity.pkl

# Python caches and local environments
__pycache__/
.pytest_cache/
*.py[cod]
.venv/
venv/

# Keep secrets private while allowing the shared theme config to be tracked
.streamlit/secrets.toml
